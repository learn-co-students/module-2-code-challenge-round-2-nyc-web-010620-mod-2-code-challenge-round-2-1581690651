<h2>Create a Heroine</h2>

<ul>
  <%= form_for @heroine do |h| %>
    <%=h.text_field :name %>
    <%=h.text_field :super_name %>
    <%=h.collection_select(:power_id, Power.all) %>
    <%= Submit %>
  <% end %>
</ul>
